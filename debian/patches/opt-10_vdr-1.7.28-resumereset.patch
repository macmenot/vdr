Index: vdr-1.7.28/dvbplayer.c
===================================================================
--- vdr-1.7.28.orig/dvbplayer.c	2012-05-06 13:02:35.000000000 +0200
+++ vdr-1.7.28/dvbplayer.c	2012-06-03 23:13:52.565011474 +0200
@@ -364,11 +364,16 @@
   if (index) {
      int Index = ptsIndex.FindIndex(DeviceGetSTC());
      if (Index >= 0) {
-        Index -= int(round(RESUMEBACKUP * framesPerSecond));
-        if (Index > 0)
-           Index = index->GetNextIFrame(Index, false);
-        else
+        int backup = int(round(RESUMEBACKUP * framesPerSecond));
+        if (Index >= index->Last() - backup)
            Index = 0;
+        else {
+           Index -= backup;
+           if (Index > 0)
+              Index = index->GetNextIFrame(Index, false);
+           else
+              Index = 0;
+           }
         if (Index >= 0)
            return index->StoreResume(Index);
         }
